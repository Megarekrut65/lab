#pragma once
#include "my_sorts.h"
namespace sorting
{
	void bubble_sort(int*, long);
	void quick_sort(int*, long);
	void merge_sort_topdown(int*, long);
	void library_sort(int*, long);
	void combined_sort(int*, long, const int);
}